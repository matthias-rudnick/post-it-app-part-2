%h4
  = link_to "All Posts", posts_path
  >> #{@category.name}
- @posts.each do |post|
  .well.span8
    %p= link_to post.title, post.url
    %p= post.description
    %p
      post by #{post.user.username}
    %br/
    %p= link_to "#{post.comments.count} comments", post_path(post)